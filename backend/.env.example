# 服务器配置
HOST=0.0.0.0
PORT=8000

# 数据库路径（相对于 backend 目录）
DATABASE_URL=sqlite:///./data/goros.db

# 货币汇率（固定近似值，每次抓取可手动更新）
RATE_USD_TO_JPY=150
RATE_HKD_TO_JPY=19.5

# 抓取调度（cron 格式，默认每天凌晨 3:00）
SCRAPE_CRON_HOUR=3
SCRAPE_CRON_MINUTE=0

# 请求配置
REQUEST_DELAY_MIN=3
REQUEST_DELAY_MAX=8
REQUEST_TIMEOUT=30
